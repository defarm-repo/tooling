{
  "name": "@defarm/example-reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sovereign reader — read a DeFarm token's verified content straight from public IPFS and Stellar, with no DeFarm API key.",
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
