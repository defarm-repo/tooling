{
  "name": "@defarm/example-notifier",
  "private": true,
  "version": "0.1.0",
  "description": "Runnable example: receipts notifier \u2014 polls the receipts feed and posts a message to a webhook (Slack/Telegram-style) when a new receipt lands.",
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@defarm/miniapp": "^0.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
